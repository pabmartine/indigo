export class Author {

    constructor(
        public id:number,
        public sort?: string,
        public total?: number,
        public title?: string,
        public description?: any,
        public image?:string
        ) {
    }
}
