export class Serie {

    constructor(
        public id?:number,
        public name?: string,
        public sort?: string,
        public total?: number,
        public image?: string
        ) {
    }
}
