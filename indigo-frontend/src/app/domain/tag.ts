export class Tag {

    constructor(
        public id?: any,
        public name?: string,
        public numBooks?: number,
        public image?: string,
        public originalImage?: string,
        public coverUrl?: string
    ) {
    }
}
